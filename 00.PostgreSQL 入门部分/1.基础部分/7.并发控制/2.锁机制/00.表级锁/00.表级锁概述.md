# 表级索
```
大多数PostgreSQL命令会自动加上恰当的锁以保证被引用的表在命令的执行过程中不会以一种不兼容的方式删除或修改.
要检查在一个数据库服务器中当前未解除的锁列表，可以使用pg_locks系统视图
不同模式的锁意味着它们有着不同的冲突模式,不同的事务中不能对同一个表持有两种冲突的锁.但是一个事务决不会和自身冲突。例如，它可以在同一个表上获得ACCESS EXCLUSIVE锁然后接着获取ACCESS SHARE锁

```
